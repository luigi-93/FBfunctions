{
  "extends": "../tsconfig.base.json",
  "exclude": ["**/test.ts", "../dist"],
  "include": [
    "../src/**/*",
    "../build/**/*"
  ],
  "compilerOptions": {
        "rootDir": "..",
        "baseUrl": "..",
        "outDir": "../dist",
        "paths": {"*": ["../node_modules/*", "build/*"]} 
      },
}